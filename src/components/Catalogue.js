import React from 'react'
import style from '../css/module/catalogue.module.css'

function Catalogue() {
  return (
    <section id='Catálogo' className={style.section}>
      <h2 className={style.title}>Catálogo</h2>
    </section>
  )
}

export default Catalogue